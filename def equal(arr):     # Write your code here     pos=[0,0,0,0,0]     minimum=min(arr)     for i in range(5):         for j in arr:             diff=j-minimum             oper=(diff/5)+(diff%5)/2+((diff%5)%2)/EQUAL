def equal(arr):
    # Write your code here
    pos=[0,0,0,0,0]
    minimum=min(arr)
    for i in range(5):
        for j in arr:
            diff=j-minimum
            oper=(diff//5)+(diff%5)//2+((diff%5)%2)//1
            pos[i]+=oper
        minimum-=1
    return min(pos)
